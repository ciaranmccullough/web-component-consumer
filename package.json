{
  "name": "chart-consumer",
  "version": "1.0.0",
  "description": "Consumer app that uses the chart web component",
  "scripts": {
    "build": "vite build",
    "dev": "vite",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "typescript": "^5.4.0",
    "vite": "^5.4.0"
  }
}
